# Development Environment Variables
# Copy this file to .env and update values as needed

# Logging
# Options: trace, debug, info, warn, error
RUST_LOG=debug

# Database (optional - defaults to patient_manager.db)
# DATABASE_URL=sqlite:patient_manager.db

# Reset database on startup (optional)
# RESET_DATABASE=false
