{
  "version": 1,
  "identifier": "default",
  "description": "Default capability allowing native dialogs, updater, and process control. The renderer no longer holds direct filesystem read/write capabilities — backend Tauri commands own all file I/O.",
  "windows": ["*"],
  "webviews": ["*"],
  "permissions": [
    "core:default",
    "dialog:default",
    "dialog:allow-open",
    "dialog:allow-save",
    "updater:default",
    "process:default"
  ]
}
